# Production Environment Variables (Example)
# These values should be set in Cloud Run environment variables or Secret Manager
# DO NOT commit actual production values to git
#
# For Cloud Run deployment:
# 1. Non-sensitive values: Set in cloud-run-*.yaml files
# 2. Sensitive values: Store in Secret Manager and reference via secretKeyRef
#
# See docs/DEPLOYMENT.md for detailed instructions

# =============================================================================
# Python API Configuration (Cloud Run)
# =============================================================================
# Set in cloud-run-python.yaml:
# PYTHON_API_HOST=0.0.0.0
# PORT=8000
# PYTHON_API_BASE_URL=https://api.icons.example.com

# =============================================================================
# Frontend Configuration (Cloud Run)
# =============================================================================
# Set in cloud-run-frontend.yaml:
# NEXT_PUBLIC_EMBEDDING_SERVICE_URL=https://api.icons.example.com
# NEXT_PUBLIC_FRONTEND_URL=https://icons.example.com
# EMBEDDING_SERVICE_URL=https://api.icons.example.com (or internal URL)
# PORT=3000
# NODE_ENV=production

# =============================================================================
# Elasticsearch Configuration (Secret Manager)
# =============================================================================
# Store in Secret Manager as "elasticsearch-api-key"
# Reference in Cloud Run YAML via secretKeyRef

# =============================================================================
# API Key Authentication (Secret Manager)
# =============================================================================
# Store API keys in Secret Manager as "api-keys" (JSON array)
# Store frontend API key in Secret Manager as "frontend-api-key"
# Reference in Cloud Run YAML via secretKeyRef

# =============================================================================
# CORS Configuration (Cloud Run)
# =============================================================================
# Set in cloud-run-python.yaml:
# CORS_ORIGINS=https://icons.example.com

# =============================================================================
# Rate Limiting (Cloud Run)
# =============================================================================
# Set in cloud-run-python.yaml:
# RATE_LIMIT_PER_MINUTE=60
# RATE_LIMIT_PER_HOUR=1000
# RATE_LIMIT_BURST=10
