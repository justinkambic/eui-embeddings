# Example Environment Variables for EUI Icon Embeddings
# Copy this file to .env.local, .env.development, or .env.production and fill in values
#
# For local development, create .env.local (gitignored)
# For Docker Compose, create .env.docker
# For production, use Cloud Run environment variables or Secret Manager

# =============================================================================
# Python API Configuration
# =============================================================================
PYTHON_API_HOST=0.0.0.0
PYTHON_API_PORT=8000
PYTHON_API_BASE_URL=http://localhost:8000

# =============================================================================
# Frontend Configuration
# =============================================================================
# Public-facing URL (used by browser)
NEXT_PUBLIC_EMBEDDING_SERVICE_URL=http://localhost:8000
NEXT_PUBLIC_FRONTEND_URL=http://localhost:3000

# Server-side URL (can differ for internal networking)
EMBEDDING_SERVICE_URL=http://localhost:8000
PORT=3000
NODE_ENV=development

# =============================================================================
# Token Renderer Configuration
# =============================================================================
TOKEN_RENDERER_HOST=0.0.0.0
TOKEN_RENDERER_PORT=3002
TOKEN_RENDERER_BASE_URL=http://localhost:3002
TOKEN_RENDERER_RATE_LIMIT=10

# =============================================================================
# Elasticsearch Configuration
# =============================================================================
ELASTICSEARCH_ENDPOINT=https://your-cluster.es.amazonaws.com
ELASTICSEARCH_API_KEY=your-elasticsearch-api-key
ELASTICSEARCH_TIMEOUT=30
ELASTICSEARCH_MAX_RETRIES=3

# =============================================================================
# API Key Authentication
# =============================================================================
# API key header name
API_KEY_HEADER=X-API-Key

# API keys (comma-separated for local, JSON array for Secret Manager)
# For local development, use comma-separated list:
API_KEYS=key1,key2,key3

# For production, use Secret Manager:
# API_KEYS_SECRET_NAME=api-keys

# Frontend API key (for frontend to authenticate with Python API)
FRONTEND_API_KEY=your-frontend-api-key

# Admin API key (optional, for admin endpoints)
ADMIN_API_KEY=

# =============================================================================
# CORS Configuration
# =============================================================================
# Comma-separated list of allowed origins
CORS_ORIGINS=http://localhost:3000,http://localhost:3001

# =============================================================================
# Rate Limiting
# =============================================================================
RATE_LIMIT_PER_MINUTE=60
RATE_LIMIT_PER_HOUR=1000
RATE_LIMIT_BURST=10

# =============================================================================
# EUI Repository Configuration
# =============================================================================
EUI_LOCATION=./data/eui
EUI_REPO=https://github.com/elastic/eui.git

# =============================================================================
# MCP Server Configuration (for local Docker usage)
# =============================================================================
# MCP_SERVER_URL=http://localhost:8000
# SEARCH_API_URL=http://localhost:8000/search
