import { typeToPathMap } from "./utils/file_to_name";

/**
 * Array of all available icon type names
 * Used for navigation between icons
 */
export const iconTypes = Object.keys(typeToPathMap);







