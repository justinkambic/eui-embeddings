# Ingester runtime dependencies.
# Phase 1 scaffold only — concrete versions get pinned in Phase 3.

httpx>=0.27           # async HTTP for ES + concurrent inference calls
resvg-py>=0.2         # deterministic SVG -> PNG rasterizer
Pillow>=10            # image handling for grayscale + normalization
python-dotenv>=1.0    # load .env in dev
elasticsearch>=9      # bulk + inference helpers

# Dev / test
pytest>=8
pytest-asyncio>=0.23
pytest-cov>=5
respx>=0.21           # httpx mock for unit tests
